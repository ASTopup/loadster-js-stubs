class json {

    /**
     * Parses a string which is in the format of a JSON obect.
     * @param {string} str - A string representation of the JSON to parse.
     */
    static parse(str);

}

const JSON = json;
const Json = json;

module.exports = {json, JSON, Json};