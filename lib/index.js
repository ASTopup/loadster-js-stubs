const {Bot, bot} = require('./bot');
const {Browser, browser, Keyboard, keyboard} = require('./browser');
const {Console, console} = require('./console');
const {Crypto, crypto} = require('./crypto');
const {Formats, formats} = require('./formats');
const {Http, http} = require('./http');
const {JSON, Json, json} = require('./json');
const {XML, Xml, xml} = require('./xml');



module.exports = {
    Bot, bot,
    Browser, browser, Keyboard, keyboard,
    Console, console,
    Crypto, crypto,
    Formats, formats,
    Http, http,
    JSON, Json, json,
    XML, Xml, xml
};